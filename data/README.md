# Data

Nothing to download. Regulatory links are fetched from CollecTRI through the
OmniPath REST endpoint on first run and cached to `results/collectri_raw.tsv`;
expression dynamics are simulated by `experiments/01_build_dataset.py`.

Every dataset is reproducible from the seeds in `src/config.py`.
